[project]
name = "arbf-engine"
version = "0.1.0"
description = "Allocation engine for the ARBF Version-1 study"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
