__pycache__/
*.pyc
.pytest_cache/

# Raw per-run records of the adversarial study (20+ MB, deterministically regenerable
# with `python -m adversarial <stage>`). The derived per-cell summaries (*.json),
# results/tables.md and the preserved-failure corpus are committed.
adversarial/results/*.jsonl
